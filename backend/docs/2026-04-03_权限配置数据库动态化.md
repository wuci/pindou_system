# 2026-04-03 权限配置数据库动态化

## 修改日期
2026-04-03

## 功能说明
将权限配置从前端硬编码改为数据库动态配置，支持灵活调整权限结构而不需要修改代码。

## 修改的文件

### 后端文件

#### 1. 新增实体类
**文件**：`backend/pdsystem-entity/src/main/java/com/pindou/timer/entity/Permission.java`

**说明**：权限配置实体类，存储权限树结构

**字段说明**：
| 字段 | 类型 | 说明 |
|------|------|------|
| id | String | 权限ID（UUID） |
| parentId | String | 父权限ID，顶级节点为root |
| permissionKey | String | 权限编码（如 table:view） |
| permissionName | String | 权限名称（显示名称） |
| permissionType | String | 权限类型：module-模块，permission-权限项 |
| icon | String | 图标名称 |
| path | String | 路由路径（可选） |
| sort | Integer | 排序 |
| status | Integer | 状态：0-禁用，1-启用 |
| isBuiltIn | Integer | 是否内置：0-否，1-是 |

#### 2. 新增 Mapper
**文件**：`backend/pdsystem-service/src/main/java/com/pindou/timer/mapper/PermissionMapper.java`

#### 3. 新增 DTO
**文件**：
- `backend/pdsystem-entity/src/main/java/com/pindou/timer/dto/PermissionResponse.java`
- `backend/pdsystem-dto/src/main/java/com/pindou/timer/dto/CreatePermissionRequest.java`
- `backend/pdsystem-dto/src/main/java/com/pindou/timer/dto/UpdatePermissionRequest.java`

#### 4. 新增 Service
**文件**：
- `backend/pdsystem-service/src/main/java/com/pindou/timer/service/PermissionService.java`
- `backend/pdsystem-service/src/main/java/com/pindou/timer/service/impl/PermissionServiceImpl.java`

**方法说明**：
| 方法 | 说明 |
|------|------|
| getPermissionTree() | 获取权限树（树形结构） |
| getAllPermissions() | 获取所有权限（平铺） |
| getPermissionDetail(id) | 获取权限详情 |
| createPermission(request) | 创建权限 |
| updatePermission(request) | 更新权限 |
| deletePermission(id) | 删除权限 |

#### 5. 新增 Controller
**文件**：`backend/pdsystem-server/src/main/java/com/pindou/timer/controller/PermissionController.java`

**API接口**：
| 接口 | 方法 | 说明 |
|------|------|------|
| /permissions/tree | GET | 获取权限树 |
| /permissions/all | GET | 获取所有权限 |
| /permissions/{id} | GET | 获取权限详情 |
| /permissions | POST | 创建权限 |
| /permissions | PUT | 更新权限 |
| /permissions/{id} | DELETE | 删除权限 |

### 前端文件

#### 1. 新增 API 文件
**文件**：`frontend/src/api/permission.ts`

**说明**：权限管理相关API接口

**接口说明**：
```typescript
// 获取权限树
export const getPermissionTree = () => http.get<PermissionResponse[]>('/permissions/tree')

// 获取所有权限列表
export const getAllPermissions = () => http.get<PermissionResponse[]>('/permissions/all')

// 获取权限详情
export const getPermissionDetail = (id: string) => http.get<PermissionResponse>(`/permissions/${id}`)

// 创建权限
export const createPermission = (data: CreatePermissionParams) => http.post<string>('/permissions', data)

// 更新权限
export const updatePermission = (data: UpdatePermissionParams) => http.put<boolean>('/permissions', data)

// 删除权限
export const deletePermission = (id: string) => http.delete<boolean>(`/permissions/${id}`)
```

#### 2. 修改 PermissionTree 组件
**文件**：`frontend/src/components/PermissionTree.vue`

**修改内容**：
- 从 API 动态加载权限树数据
- 使用 `permissionKey` 作为节点唯一标识
- 移除硬编码的权限数据

#### 3. 修改 role.ts
**文件**：`frontend/src/api/role.ts`

**修改内容**：
- 移除 PermissionTreeNode 接口定义
- 从 permission.ts 重新导出 PermissionResponse

### 数据库文件

#### 初始化脚本
**文件**：`backend/docs/db/2026-04-03_权限配置表初始化.sql`

**说明**：创建权限配置表并初始化默认权限数据

## 数据库表结构

```sql
CREATE TABLE `sys_permission` (
    `id` VARCHAR(32) NOT NULL COMMENT '权限ID（UUID）',
    `parent_id` VARCHAR(32) NOT NULL DEFAULT 'root' COMMENT '父权限ID',
    `permission_key` VARCHAR(100) NOT NULL COMMENT '权限编码',
    `permission_name` VARCHAR(50) NOT NULL COMMENT '权限名称',
    `permission_type` VARCHAR(20) NOT NULL COMMENT '权限类型',
    `icon` VARCHAR(50) DEFAULT NULL COMMENT '图标名称',
    `path` VARCHAR(200) DEFAULT NULL COMMENT '路由路径',
    `sort` INT NOT NULL DEFAULT 0 COMMENT '排序',
    `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态',
    `is_built_in` TINYINT NOT NULL DEFAULT 0 COMMENT '是否内置',
    `description` VARCHAR(200) DEFAULT NULL COMMENT '描述',
    `created_at` BIGINT NOT NULL COMMENT '创建时间',
    `updated_at` BIGINT NOT NULL COMMENT '更新时间',
    `deleted_at` TINYINT NOT NULL DEFAULT 0 COMMENT '删除标记',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_permission_key` (`permission_key`),
    KEY `idx_parent_id` (`parent_id`),
    KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='权限配置表';
```

## 初始化数据

权限树结构（10个模块）：

### 1. 工作台
- 查看工作台 (`table:view`)

### 2. 桌台管理
- 查看桌台 (`table:view`)
- 编辑桌台 (`table:update`)
- 开始计时 (`table:start`)
- 暂停计时 (`table:pause`)
- 恢复计时 (`table:resume`)
- 续费时长 (`table:extend`)
- 结束计时 (`table:end`)
- 忽略提醒 (`table:ignore`)
- 删除桌台 (`table:delete`)

### 3. 订单管理
- 查看订单 (`order:view`)
- 创建订单 (`order:create`)
- 编辑订单 (`order:update`)
- 删除订单 (`order:delete`)

### 4. 数据统计
- 查看统计 (`report:view`)

### 5. 用户管理
- 查看用户 (`user:view`)
- 新增用户 (`user:create`)
- 编辑用户 (`user:update`)
- 删除用户 (`user:delete`)
- 重置密码 (`user:resetPassword`)

### 6. 角色管理
- 管理角色 (`role:manage`)

### 7. 操作日志
- 查看日志 (`log:view`)

### 8. 会员管理
- 查看会员 (`member:view`)
- 新增会员 (`member:create`)
- 编辑会员 (`member:update`)
- 删除会员 (`member:delete`)
- 会员充值 (`member:recharge`)

### 9. 会员等级
- 管理等级 (`member:level`)

### 10. 系统设置
- 系统配置 (`system:config`)

## 使用说明

### 1. 执行数据库初始化脚本

```bash
mysql -u root -p pindou_timer < backend/docs/db/2026-04-03_权限配置表初始化.sql
```

### 2. 重启后端服务

确保新增的 Controller、Service、Mapper 生效

### 3. 前端使用

权限树组件会自动从 API 加载数据，无需修改角色管理页面。

### 4. 管理权限（待实现）

可以创建权限配置管理界面，支持：
- 新增自定义权限
- 编辑权限名称、图标、排序
- 禁用/启用权限
- 删除自定义权限（内置权限不可删除）

## 优势

1. **灵活配置**：无需修改代码即可调整权限结构
2. **动态加载**：前端从 API 加载权限树，支持热更新
3. **可扩展性**：方便添加新的权限模块
4. **数据一致性**：权限配置集中管理，避免前后端不一致
5. **内置保护**：内置权限不可删除，保证系统稳定性

## 注意事项

1. **权限编码唯一性**：数据库层面通过唯一索引保证
2. **内置权限保护**：`is_built_in=1` 的权限不可修改和删除
3. **父子关系约束**：有子权限的父节点不可删除
4. **软删除支持**：使用 `deleted_at` 字段实现软删除
5. **时间戳**：使用毫秒时间戳，与系统其他表保持一致

## 后续优化

1. **权限配置管理界面**：提供可视化的权限配置管理
2. **权限批量导入导出**：支持权限配置的批量操作
3. **权限变更历史**：记录权限配置的变更历史
4. **权限依赖关系**：定义权限之间的依赖关系
5. **数据权限**：扩展支持数据级别的权限控制

## 相关文档
- 无
