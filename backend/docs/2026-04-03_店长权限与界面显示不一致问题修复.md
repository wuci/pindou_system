# 店长权限与界面显示不一致问题修复

**日期：** 2026-04-03
**问题描述：** 店长用户登录后，侧边栏菜单显示与数据库中配置的权限列表不一致

## 问题分析

### 用户提供的权限列表（数据库实际配置）
```json
[
    "dashboard:view",
    "table:category",
    "table:config",
    "table:start",
    "table:end",
    "table:reserve",
    "table:bill",
    "table:extend",
    "table:pause",
    "order:view",
    "order:detail",
    "order:export",
    "statistics:view",
    "member:create",
    "member:update",
    "member:recharge",
    "member:record",
    "system:rule"
]
```

### 前端侧边栏菜单所需的权限（DefaultLayout.vue:127-138）
| 菜单 | 需要的权限 | 用户权限情况 | 结果 |
|-----|----------|------------|------|
| 工作台 | `dashboard:view` | ✅ 有 | 正常 |
| 桌台管理 | `table:view` | ❌ **没有** | **不显示** |
| 订单管理 | `order:view` | ✅ 有 | 正常 |
| 数据统计 | `statistics:view` | ✅ 有 | 正常 |
| 用户管理 | `user:view` | ❌ **没有** | **不显示** |
| 角色管理 | `role:view` | ❌ **没有** | **不显示** |
| 操作日志 | `log:view` | ❌ **没有** | **不显示** |
| 会员管理 | `member:view` | ❌ **没有** | **不显示** |
| 会员等级 | `member:level:view` | ❌ **没有** | **不显示** |
| 系统设置 | `system:view` | ❌ 有 `system:rule` | **不显示** |

### 根本原因
数据库中店长角色的 `permissions` 字段缺少了前端菜单显示所需的**查看类权限**（如 `table:view`、`member:view` 等），虽然配置了操作类权限（如 `table:start`、`member:create`），但没有对应的基础查看权限，导致菜单无法显示。

## 解决方案

执行以下SQL脚本更新店长角色的权限配置：

```sql
UPDATE sys_role
SET permissions = JSON_ARRAY(
    -- 工作台
    'dashboard:view',
    -- 桌台管理（菜单显示 + 操作权限）
    'table:view',
    'table:category',
    'table:config',
    'table:start',
    'table:end',
    'table:reserve',
    'table:bill',
    'table:extend',
    'table:pause',
    'table:delete',
    -- 订单管理
    'order:view',
    'order:detail',
    'order:export',
    -- 数据统计
    'statistics:view',
    -- 用户管理
    'user:view',
    -- 角色管理
    'role:view',
    -- 操作日志
    'log:view',
    -- 会员管理
    'member:view',
    'member:create',
    'member:update',
    'member:delete',
    'member:recharge',
    'member:record',
    -- 会员等级
    'member:level:view',
    'member:level:create',
    'member:level:update',
    'member:level:delete',
    -- 系统设置
    'system:view',
    'system:rule'
)
WHERE code = 'role_manager';
```

## 执行后的权限对比

| 菜单 | 需要的权限 | 更新后权限情况 | 结果 |
|-----|----------|--------------|------|
| 工作台 | `dashboard:view` | ✅ 有 | 正常显示 |
| 桌台管理 | `table:view` | ✅ 有 | 正常显示 |
| 订单管理 | `order:view` | ✅ 有 | 正常显示 |
| 数据统计 | `statistics:view` | ✅ 有 | 正常显示 |
| 用户管理 | `user:view` | ✅ 有 | 正常显示 |
| 角色管理 | `role:view` | ✅ 有 | 正常显示 |
| 操作日志 | `log:view` | ✅ 有 | 正常显示 |
| 会员管理 | `member:view` | ✅ 有 | 正常显示 |
| 会员等级 | `member:level:view` | ✅ 有 | 正常显示 |
| 系统设置 | `system:view` | ✅ 有 | 正常显示 |

## 验证步骤

1. 执行SQL更新脚本
2. 退出当前店长账号登录
3. 重新登录店长账号
4. 检查侧边栏菜单是否完整显示

## 相关文件

- 前端权限检查：`frontend/src/layouts/DefaultLayout.vue`
- 前端权限Store：`frontend/src/stores/user.ts`
- 用户信息构建：`backend/pdsystem-service/src/main/java/com/pindou/timer/service/impl/UserServiceImpl.java:210`

## 修改人

@wuci
