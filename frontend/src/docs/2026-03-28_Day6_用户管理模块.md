# 修改记录

## 日期
2026-03-28

## 修改内容
Day 6 - 用户管理模块

## 开发排期
对应 Day 6 后端和前端任务

## 实现的功能

### 后端任务

#### 1. DTO类（4个）
| 文件 | 描述 |
|------|------|
| `UserQueryRequest.java` | 用户查询请求DTO |
| `UserResponse.java` | 用户信息响应DTO |
| `CreateUserRequest.java` | 创建用户请求DTO |
| `UpdateUserRequest.java` | 更新用户请求DTO |

#### 2. UserService接口扩展
在原有 `UserService` 接口中新增方法：
- `getUserList()` - 获取用户列表（分页）
- `getAllUsers()` - 获取所有用户（不分页）
- `createUser()` - 创建用户
- `updateUser()` - 更新用户
- `deleteUser()` - 删除用户（含不能删除自己校验）
- `resetPassword()` - 重置密码

#### 3. UserServiceImpl实现
- 分页查询（支持用户名模糊、角色筛选、状态筛选）
- 用户名唯一性验证
- 密码BCrypt加密
- 角色有效性验证
- 软删除支持
- 不能删除自己校验

#### 4. UserController
| 接口 | 方法 | 功能 |
|------|------|------|
| `GET /users` | 获取用户列表（分页） |
| `GET /users/all` | 获取所有用户（不分页） |
| `POST /users` | 创建用户 |
| `PUT /users/{id}` | 更新用户 |
| `DELETE /users/{id}` | 删除用户 |
| `PUT /users/{id}/reset-password` | 重置密码 |

### 前端任务

#### 1. user.ts API扩展
- 添加 `getAllUsers()` 接口

#### 2. 用户管理页面
- 页面布局（筛选条件 + 表格 + 分页）
- 筛选功能（用户名、角色、状态）
- 用户列表表格（用户名、昵称、角色、状态、最后登录、创建时间、操作）
- 用户新增弹窗（表单验证、角色选择）
- 用户编辑弹窗（表单回显、角色选择）
- 重置密码功能（确认弹窗）
- 删除功能（确认提示）
- 分页功能

## 文件变更

### 后端文件
```
pdsystem-entity/src/main/java/com/pindou/timer/dto/
├── UserQueryRequest.java (新增)
├── UserResponse.java (新增)
├── CreateUserRequest.java (新增)
└── UpdateUserRequest.java (新增)

pdsystem-service/src/main/java/com/pindou/timer/
├── service/
│   └── UserService.java (扩展方法)
└── service/impl/
    └── UserServiceImpl.java (实现方法)

pdsystem-server/src/main/java/com/pindou/timer/controller/
└── UserController.java (新增)
```

### 前端文件
```
src/api/
└── user.ts (添加 getAllUsers)

src/views/UserManagement/
└── index.vue (完整实现)
```

## 技术要点

1. **用户名唯一性验证**
   ```java
   User existUser = getByUsername(request.getUsername());
   if (existUser != null) {
       throw new BusinessException(ErrorCode.USERNAME_EXISTS);
   }
   ```

2. **密码BCrypt加密**
   ```java
   user.setPassword(PasswordUtil.encode(request.getPassword()));
   ```

3. **不能删除自己**
   ```java
   if (userId.equals(operatorId)) {
       throw new BusinessException(ErrorCode.BAD_REQUEST, "不能删除自己");
   }
   ```

4. **软删除**
   使用 MyBatis-Plus 的 `@TableLogic` 注解

5. **状态显示**
   - 1 → 启用（绿色）
   - 0 → 禁用（红色）

## 页面功能

| 功能 | 描述 |
|------|------|
| 筛选查询 | 用户名、角色、状态 |
| 新增用户 | 用户名、密码、昵称、角色、状态 |
| 编辑用户 | 昵称、角色、状态 |
| 重置密码 | 默认密码：123456 |
| 删除用户 | 软删除，不能删除自己 |
| 分页 | 支持 10/20/50/100 条/页 |
